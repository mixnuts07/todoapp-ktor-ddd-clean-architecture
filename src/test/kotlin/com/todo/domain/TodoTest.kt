package com.todo.domain

import org.junit.Test
import kotlin.test.assertEquals

class TodoTest {
    @Test
    fun `TODOのタイトルの長さが20文字以内であること`() {
        val expected = true
        val actual = true
        assertEquals(expected, actual)
    }
}